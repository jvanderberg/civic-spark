# Fifty concurrent participants, third attempt — 2026-09-20 13:58–14:20 UTC (relay workers + events socket)

Deployment: main `98ffdaa` (round two fixes + relay workers, default 2 + per-workspace events socket + idle-terminal detach + loop counters), Machine d8d962e3b65118 performance-1x (one vCPU), same volume. Instance cleaned beforehand. Canary passed first (6m40s). Fresh identities (cohort F), 3.5 s stagger, same 16 projects.
Evidence: `artifacts/participant-load/cohort-2026-09-20T13-58-33-335Z/`, `backend.jsonl`, `telemetry.py`, this file.

## Outcome: 48 of 50 (17 → 47 → 48 across the three attempts)

| Measure | Attempt 1 (2 vCPU, old code) | Attempt 2 (1 vCPU, round two) | Attempt 3 (1 vCPU, relays + events) |
| --- | --- | --- | --- |
| Passed | 17 | 47 | 48 |
| Median time per person | n/a | 11m34s | 9m39s |
| HTTP requests in the run | ~27k (20 min) | 26,949 | 14,085 |
| HTTP errors | 6 | 1 | 0 |
| Sprite commands | 5,178 processes | 16,119 (96 % via sessions) | 7,884 (92 % via sessions) |
| Home page first byte at peak | 34.6 s | 7–11 s | 12–13 s during provisioning, then falling |
| Main loop CPU median / max | not measured | 68 % / 96 % | 38 % / 93 % |
| Relay workers CPU max | n/a | n/a | 3.6 % |
| Change notifications pushed | n/a | n/a | 2,370 |

Failures: one 30 s editor wait at the hello step during the peak (p44), and one MVP turn still streaming after the runner's 20-minute limit (p21, model behaviour). Zero HTTP errors of any kind.

## What the per-process telemetry showed

- The relay works: workers owned up to 26 agent runners, 26 helper sessions and only 7 terminal PTYs (hidden tabs now detach), at under 4 % CPU each with loop delay p99 ≤ 25 ms. The main process read zero agent lines and zero terminal chunks and spawned zero Sprite processes.
- The events socket halved request volume: 14,085 requests versus 26,949, with 2,370 pushed notifications replacing timers.
- The main loop still peaked at 85–93 % between 14:02 and 14:08. A 15 s CPU profile taken during that window attributes **79.5 % of main-loop time to `structuredClone`** in the domain engine's `snapshot()`, called from service lookups (find one event/team/participant) on nearly every request, socket authorization check, lifecycle tick and events probe. This is JavaScript time, not I/O; it is the audit's finding four and it grows with state size.
- The main process still owned 49 long-lived `sprite exec relay.py` children, the agent Git integration relay started at agent prepare (apps/server/src/integrations.ts), which the bridge did not cover.

## Fixes queued from this run

1. Zero-copy state reads: `engine.peek()` for all read-only service lookups; portal builds its own arrays; provisioning records copied. Committed on the branch (`9655f13`), full check green, not yet deployed. Expected to remove most of the 80–90 % main-loop peak.
2. Move the per-workspace Git integration relay child into the relay workers (in progress).
3. Provisioning wave: host-side Git bundling/checkout children and 13 MB/min of first-load assets remain on the main process during arrivals.
