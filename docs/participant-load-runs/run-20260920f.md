# Fifty concurrent participants, fourth attempt — 2026-09-20 15:01–15:17 UTC (zero-copy state + worker-owned Git relays)

Deployment: main `903f55d`, one performance vCPU, same Machine d8d962e3b65118 and volume, relay workers 2, events socket, zero-copy state reads, integration relays in workers. Instance cleaned beforehand; canary passed (7m50s). Fresh identities (cohort G), 3.5 s stagger.
Evidence: `artifacts/participant-load/cohort-2026-09-20T15-01-48-360Z/`, `backend.jsonl`, `telemetry.py`, this file.

## Outcome: 48 of 50, and the host is no longer the bottleneck

| Measure | Run 1 (2 vCPU, old) | Run 2 | Run 3 | Run 4 (this) |
| --- | --- | --- | --- | --- |
| Passed | 17 | 47 | 48 | 48 |
| Median time per person | n/a | 11m34s | 9m39s | 7m39s |
| Slowest person | n/a | 24m20s | 18m49s | 14m24s |
| HTTP requests | ~27k | 26,949 | 14,085 | 14,253 |
| HTTP p50 / p95 handler time | n/a | 100 / 262 ms | 108 / 393 ms | 43 / 168 ms |
| Home page first byte at peak | 34.6 s | 7–11 s | 12–13 s | 0.25 s |
| Main loop CPU median / max | n/a | 68 % / 96 % | 38 % / 93 % | 17 % / 35 % |
| Main-process Sprite children | all | all | 49 | 0 |
| Relay worker CPU max | n/a | n/a | 3.6 % | 7.2 % |
| Failures from load | 28 | 1 | 1 | 0 |

The main loop's 35 % maximum occurred during the arrival wave (provisioning Git bundling on the host); during the concurrent agent phase it sat at 15–20 %. Both failures were the stale Changes-preview Share conflict at the hello step (the helper rejected a Share whose preview fingerprint predated a refresh); they are a client/runner behaviour, not load.

## What changed since run 3
- Zero-copy state reads (`engine.peek()`) replaced a full `structuredClone` per lookup; the profile had attributed 79.5 % of main-loop time to it.
- The per-workspace agent Git relay child moved into the relay workers and now starts only while an agent or terminal session exists.

## Remaining items
1. Stale-preview Share conflict: refresh the preview and retry once in the client when nothing but timing changed; make the runner do the same.
2. Provisioning wave: host-side Git bundling/checkout and first-load asset serving (35 % peak, sub-second responses; acceptable at 50, worth watching at 60+).
3. Runner: the 20-minute agent-turn limit ends long but legitimate turns; consider a longer limit for load runs.
